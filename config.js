/**
 * Deployment-specific Trailthread settings.
 * Change the extension URL here when the package moves to a release page or another host.
 */
globalThis.TRAILTHREAD_CONFIG = Object.freeze({
  komootExtensionDownloadUrl: "./downloads/trailthread-komoot-exporthelfer.zip"
});
